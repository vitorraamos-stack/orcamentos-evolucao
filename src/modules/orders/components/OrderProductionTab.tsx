import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import type { OsOrder } from "@/features/hubos/types";
export function OrderProductionTab({ order }: { order: OsOrder }) { return <div className="grid gap-4 sm:grid-cols-2"><Card><CardHeader><CardTitle>Produção</CardTitle></CardHeader><CardContent className="space-y-2 text-sm"><p><span className="text-muted-foreground">Status:</span> {order.prod_status ?? "Ainda não iniciada"}</p><p><span className="text-muted-foreground">Tag:</span> {order.production_tag ?? "—"}</p><p><span className="text-muted-foreground">Reprodução:</span> {order.reproducao ? "Sim" : "Não"}</p><p><span className="text-muted-foreground">Letra caixa:</span> {order.letra_caixa ? "Sim" : "Não"}</p></CardContent></Card>{order.production_tag === "PRODUCAO_EXTERNA" && <Card><CardHeader><CardTitle>Produção externa</CardTitle></CardHeader><CardContent><p className="text-sm">OS identificada para produção externa.</p><p className="mt-2 text-sm text-muted-foreground">{order.insumos_details || "Sem observações adicionais."}</p></CardContent></Card>}</div>; }

