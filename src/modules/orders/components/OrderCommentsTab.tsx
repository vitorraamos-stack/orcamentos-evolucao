import { useState } from "react"; import { Button } from "@/components/ui/button"; import { Textarea } from "@/components/ui/textarea"; import type { OrderComment } from "../types/orderDetail";
export function OrderCommentsTab({ comments, loading, error, currentUserId, canComment, canRemoveAny, onCreate, onUpdate, onRemove }: { comments: OrderComment[]; loading: boolean; error: string | null; currentUserId?: string; canComment: boolean; canRemoveAny: boolean; onCreate: (message: string) => Promise<void>; onUpdate: (id: string, message: string) => Promise<void>; onRemove: (id: string) => Promise<void> }) {
  const [message, setMessage] = useState(""); const [editing, setEditing] = useState<string | null>(null); const [editMessage, setEditMessage] = useState("");
  if (loading) return <p className="text-sm text-muted-foreground">Carregando comentários…</p>; if (error) return <p className="text-sm text-destructive">{error}</p>;
  return <div className="space-y-4">{canComment && <div className="space-y-2"><Textarea value={message} maxLength={4000} onChange={e => setMessage(e.target.value)} placeholder="Adicionar comentário interno…" /><div className="flex justify-end"><Button disabled={!message.trim()} onClick={async () => { await onCreate(message); setMessage(""); }}>Comentar</Button></div></div>}{!comments.length ? <p className="rounded-lg border border-dashed p-8 text-center text-sm text-muted-foreground">Nenhum comentário interno.</p> : comments.map(comment => { const own = comment.user_id === currentUserId; return <article key={comment.id} className="rounded-lg border p-4"><div className="flex flex-wrap justify-between gap-3"><div><strong className="text-sm">{comment.user?.name ?? "Usuário"}</strong><time className="ml-2 text-xs text-muted-foreground">{new Date(comment.created_at).toLocaleString("pt-BR", { timeZone: "America/Sao_Paulo" })}</time></div><div>{own && <Button size="sm" variant="ghost" onClick={() => { setEditing(comment.id); setEditMessage(comment.message); }}>Editar</Button>}{(own || canRemoveAny) && <Button size="sm" variant="ghost" onClick={() => onRemove(comment.id)}>Remover</Button>}</div></div>{editing === comment.id ? <div className="mt-3 space-y-2"><Textarea value={editMessage} maxLength={4000} onChange={e => setEditMessage(e.target.value)} /><div className="flex justify-end gap-2"><Button variant="outline" size="sm" onClick={() => setEditing(null)}>Cancelar</Button><Button size="sm" disabled={!editMessage.trim()} onClick={async () => { await onUpdate(comment.id, editMessage); setEditing(null); }}>Salvar</Button></div></div> : <p className="mt-3 whitespace-pre-wrap text-sm">{comment.message}</p>}</article>; })}</div>;
}
