import { describe, expect, it } from "vitest"; import { formatOrderActivity, scopeLabel } from "./OrderHistoryTab";
describe("order history formatting", () => { it("humanizes scopes and removals", () => { expect(scopeLabel("PRODUCTION")).toBe("Produção"); expect(formatOrderActivity({ type: "assignee_changed", payload: { scope: "ART", to: null } })).toBe("Responsável de Arte removido"); }); it("describes transitions", () => { expect(formatOrderActivity({ type: "status_change", payload: { from: "Em Criação", to: "Para Aprovação" } })).toContain("Em Criação para Para Aprovação"); }); });
